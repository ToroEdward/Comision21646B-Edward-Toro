// controlador para traer todas las tareas
const ctrlGetPost = (req, res)

// controlador para crear una tarea
const ctrlCreatePost = (req, res)

// controlador para modificar una tarea
const ctrlUpdatePost = (req,res)

// controlador para eliminar una tarea
const ctrlDeletePost = (req, res)